--- a/lib/main.dart
+++ b/lib/main.dart
@@ -269,6 +269,10 @@ class ImageGalleryApp extends StatelessWidget {
   Widget build(BuildContext context) {
     return MultiProvider(
       providers: [
+        // Provide ThemeManager
+        ChangeNotifierProvider<ThemeManager>.value(
+          value: initializer.themeManager,
+        ),
         // Provide ModeManager
         ChangeNotifierProvider<ModeManager>.value(
           value: initializer.modeManager,
@@ -286,13 +290,17 @@ class ImageGalleryApp extends StatelessWidget {
           ),
         ),
       ],
-      child: MaterialApp(
-        title: 'Image Gallery Viewer',
-        theme: ThemeData(
-          colorScheme: ColorScheme.fromSeed(seedColor: Colors.blue),
-          useMaterial3: true,
-        ),
-        home: const AppHome(),
-        debugShowCheckedModeBanner: false,
+      child: Consumer<ThemeManager>(
+        builder: (context, themeManager, child) {
+          return MaterialApp(
+            title: 'Image Gallery Viewer',
+            theme: AppTheme.lightTheme,
+            darkTheme: AppTheme.darkTheme,
+            themeMode: themeManager.themeMode,
+            home: const AppHome(),
+            debugShowCheckedModeBanner: false,
+          );
+        },
       ),
     );
   }
